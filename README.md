# Personal QMK Layouts

## Purpose
QMK recently added support for [external userspaces](https://docs.qmk.fm/newbs_external_userspace). This repo is based off the [QMK Userspace template](https://github.com/qmk/qmk_userspace).
The Github Action handles building the firmware and produces the firmware as a build artifact.
